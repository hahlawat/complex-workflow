name: ci workflow
on: push
jobs: 
  build-project:
       runs-on: ubuntu-latest
       steps:
          - name: checkout code
            run: git clone https://github.com/hahlawat/complex-workflow.git
  
          - name: view checkedout code 
            run: cd complex-workflow/ && ls -a

          - name: add a new file 
            run: touch happy 
            

      
          

       